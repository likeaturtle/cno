# Tailcat 使用注意事项（DERP / genkey / serve）

> 本文整理自关于「如何指定 DERP、genkey 用途、如何在 serve 中使用 key、自建 derper」的讨论。
> 涉及的源码位置：`cmd/tailcat/tailcat.go`、`tailcat.go`、`wire.go`。

## 1. 主机名（hostname）是什么

对于地址 `https://derper.flashatom.online:13477/`：

| 部分 | 值 |
|---|---|
| 协议 scheme | `https` |
| **主机名 hostname** | **`derper.flashatom.online`** |
| 端口 port | `13477` |
| 路径 path | `/` |

主机名 = 协议与端口之间的那段域名。给 `--region` 时**只填 `derper.flashatom.online`**，
不要带 `https://`、不要带 `:13477`、不要带结尾 `/`。

## 2. genkey 是做什么的

`tailcat genkey` 生成并保存一个 **tailcat 身份密钥文件**，并打印其对应的 tailcat 地址。
它是唯一「造身份」的命令——`serve` 和客户端模式本身不生成，只加载。

一个 key 文件里打包了（`tailcat.PrivateKey` / `ConnInfo`）：

- WireGuard 节点私钥 → 公钥即地址里不可猜的 `ServerPublic`；
- 由它派生的 disco（路径发现）密钥对 `ServerDiscoPublic`；
- 随机 WireGuard 预共享密钥 `PresharedKey`（除非 `--psk=false`）；
- **DERP region**（`RegionID` 整数，或嵌入的 `Region` 节点信息）。

这些正是「带外交换连接元数据」的全部内容：客户端只拿到一个 tailcat 地址即可建隧道。

### 四种模式

| 命令 | 作用 |
|---|---|
| `genkey --key=<name>` | 生成**服务器** key + 地址，存到 `$CONFIG/tailcat/keys/<name>.private.json` |
| `genkey --client --key=<name>` | 生成**客户端**身份 key（无 DERP region），打印公钥，供服务器 `--allow` 用 |
| `genkey --list` | 列出已保存的 key 名 |
| `genkey --delete --key=<name>` | 删除某个 key |
| `genkey --region=list` | 列出可选 DERP region |

**为什么需要它：**

1. **稳定身份**：`default` 是 magic 名，`serve` 启动时若存在就自动加载。因为公钥固定，
   地址也固定——想把它发布到 DNS 就必须如此。客户端 magic 名是 `client-default`。
2. **鉴权**：服务器可用 `--allow=<客户端公钥...>` 只允许特定客户端。
3. **多重身份**：用 `--key=<名字>` 保存多个 key，serve 时用 `--key=<名字>` 指定，互不干扰。

生命周期：`genkey` 造 key（决定公钥、PSK、DERP）→ 服务器 `serve` 加载 → 客户端拿地址连接。

## 3. 生成的 key 怎么在 serve 里用

`--key` 是 **root 级 flag**，直接给 `serve` 加上即可（放 `serve` 前或后都行）。

| `--key` 值 | 含义 |
|---|---|
| 省略 | 若 `$CONFIG/tailcat/keys/default.private.json` 存在 → 用 `default`；否则 → `new`（进程内临时） |
| `<名字>`（无斜杠） | 读 `$CONFIG/tailcat/keys/<名字>.private.json` |
| 含 `/` 或 `\` | 直接当文件路径读 |
| `new` | 强制进程内临时 key，忽略已存的 `default` |

```sh
# 用 magic 名 default —— serve 自动加载，无需写 --key
tailcat genkey --key=default
tailcat serve 22

# 用命名的 key
tailcat genkey --key=web --region=nyc
tailcat serve --key=web 22
tailcat --key=web serve 22          # 也可放前面

# 用路径
tailcat genkey --key=./web.private.json
tailcat serve --key=./web.private.json 22

# 强制临时 key
tailcat serve --key=new 22
```

**要点：**

- **区分是哪个 `--key`**：`genkey` 用它自己的 `--key`，且**必须写在 `genkey` 之后**，
  否则报 `genkey's --key argument must be after "genkey"`；`serve`/客户端用的是 root 的 `--key`。
- 客户端侧同理，magic 名是 `client-default`。
- 加载时校验：`--psk` 默认 true，若 key 文件无 PSK 会报
  `key file ... has no WireGuard pre-shared key`。
- serve 启动会打印实际用的 key：
  `# 🐈 Server listening with saved key "web": <地址>`；加 `--verbose` 看更多。

## 4. 如何指定 DERP（两种粒度）

### 4.1 指定单个/自建 DERP 服务器（`genkey --region`）

```sh
tailcat genkey --key=default --region=302          # region ID
tailcat genkey --key=default --region=nyc          # 代号/子串
tailcat genkey --key=default --region=derp.example.com   # 自建 relay 主机名（可逗号分隔多个）
tailcat genkey --key=default --region=list         # 列出所有 region
tailcat genkey --key=default --fixed-region         # 现在探测一次最近 region，烘焙进 key
tailcat genkey --key=default --region=901 --embed-derp-map  # 把 region 节点信息嵌进地址
```

- `--region=auto`（默认）：每次服务器启动时按延迟挑最近的一个。
- `--fixed-region`：现在探测一次最近 region，烘焙进密钥，之后启动/客户端不再重探。
- `--embed-derp-map`：把 DERP map 节点信息嵌进地址（需现在选定 region，隐含 `--fixed-region`），
  客户端免去抓 map 的往返。
- `--fixed-region` 与 `--region` 互斥；`--embed-derp-map` 与 `--region=auto` 互斥。

### 4.2 指定一整张 DERP map（fleet 场景，两端都要指）

```sh
tailcat --derpmap-url=https://derp.example.com/derpmap.json genkey --key=default
tailcat --derpmap-url=https://derp.example.com/derpmap.json serve 22
```

默认值是 `https://tailcat.dev/derpmap.json`。`--derpmap-url` 是 root 级 flag，
也可用环境变量 `TAILCAT_DERPMAP_URL` 设默认值。

客户端侧也可用 `serve --full-address` / `tailcat resolve` 生成带完整 DERP 信息的自包含地址，
从而不依赖 map 抓取。

## 5. 临时指定 DERP

`--region` 只在 `genkey` 上且一定会写盘，`serve` 启动时只能读已保存 key 里烘焙的 region。
所以「临时」分两种：

**A. 临时换整张 DERP map（不碰 key，最干净）**

```sh
tailcat --derpmap-url=https://derp.example.com/derpmap.json serve 22
# 或
TAILCAT_DERPMAP_URL=https://derp.example.com/derpmap.json tailcat serve 22
```

此时 region 只能 `auto`（按这张 map 挑最近的），**没法钉死具体 region**。
想连身份也临时（不落盘）：加 `--key=new` 强制进程内临时密钥：

```sh
TAILCAT_DERPMAP_URL=https://... tailcat serve --key=new 22
```

**B. 临时钉一个具体 region，又不动 `default` key**

换个 key 名、用完删：

```sh
tailcat genkey --key=tmp --region=nyc
tailcat serve --key=tmp 22
tailcat genkey --delete --key=tmp        # 用完清理
```

**客户端临时**：直接 `--derpmap-url=...`；或先 `tailcat resolve <short-addr>` 生成自包含地址。

## 6. 自建 derper / 非标准端口（核心坑）

### 6.1 端口 443 时：最省事

DERP 客户端连接的 TCP 端口**永远取自节点的 `DERPPort` 字段，为 0 时默认 443**。
所以 derper 监听在 443 时，`--region=<主机名>` 就够了，无需 `derpmap.json`：

```sh
tailcat genkey --key=default --region=derp.example.com
tailcat serve 22
```

这条路径**不抓 DERP map**，主机名直接嵌进地址，客户端零 flag。

### 6.2 非标准端口时：必须用 map 方案

`--region=<主机名>` 这条快捷路径**只设置 `HostName`，不设端口**。所以：

- `--region=derper.flashatom.online` → 会去连 `:443`，连不上；
- `--region=derper.flashatom.online:13477` → HostName 里带冒号，`JoinHostPort` 会拼出
  `[derper.flashatom.online:13477]:443`，同样坏掉。

**必须自己写一份带 `DERPPort` 的 DERP map，用 `--derpmap-url` 指过去。**

#### 实施步骤（已实测验证）

**1. 写 `derpmap.json`**（region ID 用 900–999，这是给自建节点的保留区间）：

```json
{
  "Regions": {
    "901": {
      "RegionID": 901,
      "RegionCode": "flash",
      "RegionName": "FlashAtom",
      "Nodes": [
        {
          "Name": "901a",
          "RegionID": 901,
          "HostName": "derper.flashatom.online",
          "DERPPort": 13477
        }
      ]
    }
  }
}
```

`STUNPort` 不用写，`0` 即默认 3478（与 derper 默认一致）。

**2. 把该文件挂到一个 HTTP(S) 可达的 URL。** 临时做法（只要 genkey 那一下能访问到）：

```sh
cd /你放derpmap.json的目录
python3 -m http.server 8000      # → http://127.0.0.1:8000/derpmap.json
```

**3. genkey，把 region 嵌进 key/地址：**

```sh
tailcat --derpmap-url=http://127.0.0.1:8000/derpmap.json \
  genkey --key=default --region=901 --embed-derp-map
```

`--region=901` 是数字，直接当 RegionID 用，不必和 RegionCode 匹配。

**4. 核对结果（关键一步）：**

```sh
tailcat parse <上一步打印出的地址>
```

输出中应含：

```json
"Region": [
  { "Nodes": [ { "HostName": "derper.flashatom.online", "DERPPort": 13477 } ] }
]
```

看到 `DERPPort: 13477` 才说明端口已正确嵌入。

**5. serve**（因 region 已嵌在 key 里，`Expand` 是 no-op，**不再需要 `--derpmap-url`**）：

```sh
tailcat serve 22
```

**6. 客户端**：直接用地址，**零 flag**。

#### 实测结论

`--embed-derp-map` 把整个 region（含 `DERPPort`）烘焙进 key 和地址。
之后 serve 和客户端**都不再需要 map**——即使用一个打不通的 `--derpmap-url` 跑 serve，
依然能正确选中 region：

```
# Selected bootstrap relay region 901, FlashAtom
```

**所以那份 map 只需要「暂时可达一次」（genkey 时）；genkey 完即可撤掉。**

#### 备选：不嵌入，两端都带 map

```sh
tailcat --derpmap-url=https://<稳定URL>/derpmap.json genkey --key=default --region=901
tailcat --derpmap-url=https://<稳定URL>/derpmap.json serve 22
client$ tailcat --derpmap-url=https://<稳定URL>/derpmap.json ssh <地址>
```

代价：该 URL 必须长期稳定可达，且两端每次都要能访问。故推荐 `--embed-derp-map`。

### 6.3 自建 derper 相关命令（外部，来自 tailscale 仓库）

```sh
# 自建 relay 主机名（可逗号分隔多个）
tailcat genkey --key=default --region=derp.example.com
tailcat serve 22
```

DERP 地址会嵌入 relay 主机名，因此客户端无需额外 flag、也不碰 Tailscale 的 DERP map。
若有一整个 relay 集群，则自建 DERP map JSON，两端用 `--derpmap-url` 指过去。

## 7. 其它注意事项

- **TLS 证书**：DERP 节点默认要求证书匹配 `HostName`。若能以 `https://<主机名>/` 正常访问，
  说明证书没问题。自签则需把 `CertName` 设为 `sha256-raw:<证书SHA256>`。
- **STUN**：DERP 中继不依赖 STUN；但要升级成直连（NAT 打洞）就用到 STUN。
  derper 默认 STUN 在 UDP 3478（`cmd/derper/derper.go`），节点 `STUNPort=0` 即默认 3478。
  只有改过 `--stun-port` 时，才需在节点里加 `"STUNPort": <端口>`。
- **只用自建 relay**：map 里只放一个 region，客户端也只会用它，不会接触 Tailscale 默认服务器。
- **map JSON 必须是 HTTP(S) 可达**：`fetchDERPMap` 走 `http.DefaultClient`，`file://` 无效。
- **RegionID 范围**：900–999 保留给终端用户自建 DERP 节点；RegionID 必须非零正数。

## 8. 一句话速查

| 场景 | 做法 |
|---|---|
| derper 在 443 | `genkey --region=<主机名>`，无需 map |
| derper 在非标准端口 | 自建 `derpmap.json` 带 `DERPPort`，`--derpmap-url` + `--embed-derp-map` |
| 多个 relay / 集群 | 自建 DERP map，两端 `--derpmap-url` |
| 临时换 map | `TAILCAT_DERPMAP_URL=... tailcat serve ...`（region 只能 auto） |
| 临时钉 region | 用临时 key 名：`genkey --key=tmp --region=...` + `serve --key=tmp`，用完 `--delete` |
| 用已存 key 启动 | `tailcat serve [--key=<名字>] <端口>`（`default` 会自动加载） |
